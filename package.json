{
  "name": "node-site",
  "version": "1.0.0",
  "description": "NODE\r 1. Using note allows us to use javascript on our server.\r 2. Node uses a non-blocking event mode - which makes it run quickly.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  }
}
